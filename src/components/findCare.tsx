import React from "react";
import NavBar from "./nav";

export interface FindCarePageProps {
    
}
 
const FindCarePage: React.FunctionComponent<FindCarePageProps> = (props) => {
    return (  
        <div>
            <NavBar/>
        </div>
    );
}
 
export default FindCarePage;